# Workspace Agent Rules

## Context Exclusions

Do NOT read, reference, or include any files under `RnD/dev-docs/claude/` as context when responding to user requests. Treat that directory as if it does not exist.
